1  数据库的优化
通过查询缓存进行优化。查询走缓存，缓存中没有找到，在磁盘中查询，删除或者修改清空缓存。
为搜索字段建立索引，尤其是经常排序，查询，分组的字段应该使用索引。
使用paparperStatement
为每张表添加一个id字段，而是是自增的。
当只查询一条记录的时候或者少量记录的使用，使用limit
避免使用select *，需要什么，取什么。
如果我们提前知道表中字段的取值是有范围的，进行使用enum类型代替varchar类型，事实上enum保存的是tinyint，外表显示的是varchar，这样可以提高查询效率。